# Test Application Properties

# Test MongoDB Configuration
spring.data.mongodb.database=urlshortener_test

# Disable MongoDB transactions for tests
spring.data.mongodb.auto-index-creation=true

# Test logging
logging.level.com.urlshortener=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# Test application settings
app.base-url=http://localhost:8080/
app.short-code-length=6
app.default-expiry-days=7

# Disable Spring Boot banner for cleaner test output
spring.main.banner-mode=off